package org.test.GamePlane.game;

import android.graphics.Bitmap;

public class Explosion extends AnimationSprite{
    public Explosion(Bitmap bitmap, int segment, int speed) {
        super(bitmap, segment, speed);
    }
}
